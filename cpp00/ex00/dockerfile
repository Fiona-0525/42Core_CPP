FROM ubuntu:22.04

# 安装编译工具
RUN apt-get update && apt-get install -y \
    g++ \
    make \
    && rm -rf /var/lib/apt/lists/*

# 设置工作目录
WORKDIR /app

# 复制项目文件
COPY . .

# 编译
RUN make

# 运行
CMD ["./megaphone", "hello world"]
